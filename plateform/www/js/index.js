var plateform

document.addEventListener('deviceready', function () {
    plateform = {
        nom: "nom plafeorm"
    }
    // compas()
    // camera()
    // vibration()
    // battery()
}, false);

/*/ Nous allons créer deux fonctions; l
//a première fonction génère //
l'accélération actuelle et l'autre vérifie les 
changements d'orientation. Vous pouvez voir 
que nous utilisons à nouveau l'option fréquence pour 
surveiller les changements qui se produisent toutes les trois secondes.
*/

// function vibration(argument) {}
// function battery(argument) {}
// function camera(argument) {}
